# Legacy Design Backup

All legacy design code for `index.html`, `contact.html`, and `styles.css` is securely backed up on the `main` branch. 
We have transitioned to the `v2-formal` branch to begin the new UI/UX from scratch.
